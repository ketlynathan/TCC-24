[tool.poetry]
name = "tcc"
version = "0.1.0"
description = "exibir dados"
authors = ["Ketlyn Athan"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
streamlit = "^1.39.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
